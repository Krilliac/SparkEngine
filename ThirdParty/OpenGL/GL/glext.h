/**
 * @file glext.h
 * @brief Minimal GL extensions stub — fallback for when libgl-dev is not installed.
 *
 * SDL2's CheckOpenGL macro requires <GL/glext.h> to exist. GLAD provides all
 * actual extension loading at runtime, so this stub only needs to compile.
 */
#ifndef __glext_h_
#define __glext_h_

#endif /* __glext_h_ */
