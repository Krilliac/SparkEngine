/**
 * @file glxext.h
 * @brief Minimal GLX extensions stub — fallback for when libgl-dev is not installed.
 *
 * SDL2 may include <GL/glxext.h> alongside <GL/glx.h>. All necessary extension
 * constants and typedefs are already provided in our minimal glx.h, so this file
 * only needs to compile.
 */
#ifndef __glxext_h_
#define __glxext_h_

#endif /* __glxext_h_ */
