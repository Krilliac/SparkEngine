{
  "schemaVersion": "evidence-gaps-v1",
  "comment": "Every stable-v1 evidence type now has a required same-workflow producer and an Ubuntu rooted consumer. The lifecycle and profile-scoped package-smoke paths still require a successful hosted exact-SHA run before they become release proof; an empty ledger records that no evidence type is merely excused locally.",
  "gaps": []
}
