# TERRAFRONT W5 onboarding: screenshot the Login screen. TFLoginFlow renders
# unconditionally as the pre-world menu (Main.cpp OnImGui, Task 6) -- no
# tf_host/tf_connect needed to see it; launching the game drops the player on
# this screen instead of straight into the war.
# Run: SparkEngine.exe -game SparkGameMMOFPS.dll -exec Tools/tf_onboard_shot.cfg -test-seconds 5 -window-size 1280 720
t3 gfx_screenshot Screenshots/onboarding_login.png
