# MemorySanitizer ignorelist for SparkEngine
#
# `src:` entries disable MSan checks AND shadow propagation inside code that is
# compiled with MSan (every load in a matching file is treated as initialised).
# They cannot affect prebuilt shared libraries or runtime interceptors; the C++
# runtime is handled by linking the MSan-instrumented libc++ that
# build-linux-msan builds (see .github/msan/libcxx-runtime.cmake). Keep this
# list short: every entry is a blind spot, and Tests/TestMSanCanary.cpp must
# never match one.
#
# Syntax: https://clang.llvm.org/docs/SanitizerSpecialCaseList.html

# Jolt Physics (in-tree, proving-phase blind spot; retire in phase 3)
src:*/ThirdParty/Physics/JoltPhysics/*

# ImGui (in-tree; imgui_impl_sdl2 is never called from tests)
src:*/ThirdParty/UI/imgui/*
src:*/imgui.cpp
src:*/imgui_*.cpp

# tinyobjloader (retire early in phase 3: parsed output feeds engine code)
src:*/ThirdParty/Utils/tinyobjloader/*

# miniz (retire first in phase 3: decompression output feeds engine code)
src:*/ThirdParty/Utils/miniz/*

# AngelScript add-ons only. The core is compiled from
# build/ThirdParty/AngelScriptPatched/source/ (CMakeLists.txt:936-971) and is
# instrumented by design; a native-trampoline artefact gets a narrow fun:
# entry, never src: on the core.
src:*/ThirdParty/Scripting/angelscript-mirror/*

# stb headers (stb_image, etc.)
src:*/stb_*.h

# miniaudio
src:*/miniaudio.h
